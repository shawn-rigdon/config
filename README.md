# config
development env config

## Install
For now, only debian linux is supported.  To install the env run
```
cd linux
./install.sh
```

## Github Copilot
To enable copilot support, open Neovim and execute the command `:Copilot setup`.  Follow the instructions to link your account.  Then execute `:Copilot enable`
